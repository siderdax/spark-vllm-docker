# Qwen3.8-27B MTP(Multi-Token Prediction) 최적화 및 벤치마크 리포트

**작성일**: 2026-08-22  
**환경**: NVIDIA DGX Spark 2-Node Cluster (`raven` + `quaker`, GB10 Direct RoCE Link, TP=2)  
**서빙 엔진**: vLLM with FlashInfer, FastSafetensors, Ray Distributed Backend  
**저장 대상**: `fregata:~/Documents/qwen3.8-mtp-optimization-report.md`

---

## 1. 개요 및 요약 (Executive Summary)

본 리포트는 DGX Spark 2노드 클러스터 환경에서 **Qwen3.8-27B** 모델의 서빙 최적화, 정밀도별 성능 비교(공식 FP8 vs Unsloth NVFP4), 그리고 **MTP(Multi-Token Prediction) 내장 투기적 디코딩(Speculative Decoding)** 적용 및 토큰 수 최적화 결과를 정리한 전용 기술 보고서입니다.

### 핵심 요약
1. **Qwen 공식 FP8 vs Unsloth NVFP4 1:1 정밀 대조**:
   * MTP 미적용(No MTP) 상태에서는 NVFP4가 가중치 절감 및 4비트 FFN 연산으로 FP8 대비 31% 빨랐으나(16.4 vs 12.5 tok/s), **MTP(투기적 디코딩)를 적용하자 속도 순위가 역전**되었습니다.
   * **Qwen 공식 FP8 + MTP 4**: **`21.2 ~ 22.0 tok/s` 🏆** (스텝당 **3.04 ~ 3.15개** 토큰 생성, 4th 적중률 **31.4% ~ 37.2%**)
   * **Unsloth NVFP4 + MTP 4**: **`19.8 tok/s`** (스텝당 **2.53 ~ 2.91개** 토큰 생성, 4th 적중률 **18.0% ~ 28.9%**)
2. **양자화 노이즈와 MTP 적중률 상관관계**:
   * MTP 보조 헤드는 원래 깨끗한 FP8/BF16의 은닉 상태(Hidden State)를 기반으로 훈련되었습니다.
   * NVFP4는 MLP 가중치 양자화 노이즈로 인해 4번째 토큰 적중률이 18~28%로 급락하여, **NVFP4에서는 MTP 4의 드래프트 오버헤드가 이득을 상쇄하므로 MTP 3이 4보다 더 효율적**이라는 결론이 도출되었습니다.
   * 반면, **공식 FP8은 깨끗한 정밀도 덕분에 MTP 4에서도 31~37%의 높은 적중률을 유지**하여 실질 최고 속도를 기록했습니다.
3. **최종 프로덕션 채택**:
   * 원본 BF16 대비 100% 무손실 품질과 21~22 tok/s의 최고 속도를 동시에 보장하는 **`Qwen3.8-27B-FP8 + MTP 4`를 최종 프로덕션 구성으로 확정**하였습니다.

---

## 2. 벤치마크 결과 상세 (Benchmark Metrics)

### 2.1 Qwen3.8-27B 설정별 스피드 비교

| 순위 | 모델 및 설정 | 실측 속도 | 토큰당 소요 시간 | 스텝당 생성 효율 | MTP 4 적중률 | 비고 / 추천도 |
| :---: | :--- | :---: | :---: | :---: | :---: | :--- |
| **🥇 1위** | **Qwen3.8-27B FP8 + MTP 4** | **`21.2 ~ 22.0 tok/s`** | **0.045 ~ 0.047초** | **`3.04 ~ 3.15개`** | **31.4% ~ 37.2%** | **최종 채택 🌟 (품질 100% + 최고 속도)** |
| **🥈 2위** | **Qwen3.8-27B FP8 + MTP 3** | **`19.0 ~ 20.0 tok/s`** | 0.050 ~ 0.052초 | `2.74개` | - | 안정 세팅 |
| **🥉 3위** | **Qwen3.8-27B Unsloth NVFP4 + MTP 4** | **`19.8 tok/s`** | 0.0506초 | `2.53 ~ 2.91개` | 18.0% ~ 28.9% | MTP 4 효율 저하 (MTP 3 권장) |
| **4위** | **Qwen3.8-27B Unsloth NVFP4 (No MTP)** | **`16.4 tok/s`** | 0.0610초 | `1.00개` | - | 순수 가중치 읽기 기본 |
| **5위** | **Qwen3.8-27B FP8 (No MTP)** | **`12.5 tok/s`** | 0.0800초 | `1.00개` | - | 순수 가중치 읽기 기본 |

---

### 2.2 동일 작업/구간 1:1 정밀 대조 (FP8 vs NVFP4)

동일한 복합 추론/코딩 세션 초반 1,000 스텝 기준 위치별 수락률:

| 위치별 수락률 | **Unsloth NVFP4** (1,102 스텝) | **Qwen 공식 FP8** (921 스텝) | **FP8 우위 격차** |
| :--- | :---: | :---: | :---: |
| **1번째 토큰 (`pos 0`)** | 66.0% | **75.6%** | **+9.6%p 🏆** |
| **2번째 토큰 (`pos 1`)** | 42.0% | **55.2%** | **+13.2%p 🏆** |
| **3번째 토큰 (`pos 2`)** | 27.0% | **41.4%** | **+14.4%p 🏆** |
| **4번째 토큰 (`pos 3`)** | **18.0%** | **31.4%** | **+13.4%p 🏆** |
| **스텝당 토큰 생성량** | **2.53 tokens/step** | **3.035 tokens/step** | **FP8이 +20% 더 많이 생성** |

---

### 2.3 MTP 토큰 수($K$)별 효율 분석 및 스위트 스팟

| Speculative Tokens ($K$) | 4th / 5th 적중률 | 스텝당 생성 토큰 | 오버헤드 대비 실익 | 평가 |
| :---: | :---: | :---: | :---: | :--- |
| **$K = 2$** | - | 약 2.31개 | 매우 낮음 | 일반 자연어 대화용 보수적 세팅 |
| **$K = 3$** | - | 약 2.74개 | 낮음 | NVFP4에 적합한 구성 (+55%) |
| **$K = 4$ (선택 🌟)** | **31.4% ~ 37.2%** | **약 3.04 ~ 3.15개** | **최적** | **Qwen3.8-FP8 최적의 스위트 스팟 (+70%~76%)** |
| **$K = 5$** | ~22.9% (예상) | 약 3.25개 | 높음 (5회 드래프트) | 22% 한계 이득 대비 드래프트 비용으로 실익 미미 |

---

## 3. 핵심 기술 분석 (Technical Insights)

### 3.1 MTP의 수학적 무손실(Lossless) 보장
* **검증 원리**: 경량 MTP 헤드가 후보 토큰 $K$개를 미리 추측(Draft)하면, 본체 모델이 1회의 Forward Pass에서 이를 병렬 검증(Rejection Sampling)합니다.
* **품질 불변**: 검증 단계에서 본체 모델의 확률 분포와 일치하지 않는 후보는 즉시 탈락하고 본체 모델의 정답 토큰으로 교체되므로, **MTP 미적용 상태와 100% 동일한 출력 품질**을 유지합니다.

### 3.2 암달의 법칙과 FP8의 속도 역전 메커니즘
* MTP 미사용 시에는 토큰당 가중치 메모리 읽기 시간 비중이 100%여서 NVFP4가 빨랐으나,
* MTP 4를 적용하면 **가중치 읽기 횟수가 1/3 이하로 줄어들고 MTP 적중률의 영향력이 지배적**이 됩니다.
* FP8의 높은 적중률(31~37%)이 NVFP4의 낮은 적중률(18~28%)을 압도하여 **FP8 + MTP 4가 최종 속도에서도 승리**하게 됩니다.

---

## 4. Qwen3.8-27B 서빙 가이드 및 실행 커맨드

### 4.1 적용된 레시피
* `recipes/qwen3.8-27b-fp8.yaml` (`num_speculative_tokens: 4` 기본 설정)

### 4.2 실행 커맨드

```bash
# 1. Qwen3.8-27B-FP8 클러스터 TP2 기본 실행 (MTP 4 자동 적용, ~22.0 tok/s) 🚀
./run-qwen3.8-27b.sh

# 2. Qwen3.8-27B-FP8 단일 노드 Solo 실행 (MTP 4 자동 적용)
./run-qwen3.8-27b-solo.sh

# 3. MTP 비활성화가 필요한 경우
./run-qwen3.8-27b.sh --no-mtp

# 4. 토큰 수 수동 지정 (예: 3토큰)
./run-qwen3.8-27b.sh --speculative-tokens 3
```

---
*Updated on 2026-08-22 by Antigravity Assistant on raven (DGX Spark Cluster)*
